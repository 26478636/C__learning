sudo apt update
// sudo         switch user root     切换成root用户下
// apt          管理软件的包
// apt update   将管理软件的包更新 

sudo apt install build-essential gdb  // 通过以下命令，安装编译器和调试器

gcc --version
g++ --version
gdp --version

sudo apt install cmake
cmake --version