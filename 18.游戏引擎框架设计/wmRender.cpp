#include "wmRender.h"
#include "wmNode.h"

USING_NS_WM;

void wmRender::draw(wmNode *node)
{
    // 调用图形库，对界面进行渲染，渲染图像x，y，并把名字打印出来
    // ------------------------------------------------------------------------------------------------
    WMBLOG("Render Node x=%f, y=%f ,name=%s\n", node->_x, node->_y, node->_name.c_str());
    // ------------------------------------------------------------------------------------------------
}